[package]
name = "elixor-system"
version = "0.0.0"
edition = "2021"
exclude = ["native.node"]

[lib]
crate-type = ["cdylib"]

[dependencies]
num_cpus = "1"
is_elevated = "0.1.2"

[dependencies.neon]
version = "0.9"
default-features = false
features = ["napi-6"]
