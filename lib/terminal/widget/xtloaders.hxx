#pragma once

#include "runtime.hxx"

namespace exolix {
    /**
     * A class used for creating loader animations in the command
     * line to indicate process trees and task trees, etc...
     */
    class TerminalLoader : public TerminalXtWidget {
    private:

    public:

    };
}
