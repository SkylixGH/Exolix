#pragma once

#include <string>

namespace skylix::exolix::exolix {
    class BuildConfig {
    public:
        const std::string llcMinVersion = "14.0.6";
        const std::string lldMinVersion = "14.0.6";
    };
}